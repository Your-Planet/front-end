import { Box, styled } from "@mui/material";

export const StyledFormBoxInJoin = styled(Box)`
	display: flex;
	flex-direction: column;
	margin-top: 3rem;
	gap: 1rem;
`;

export const StyledBoxInJoin = styled(Box)`
	max-width: 520px;
	margin-left: auto;
	margin-right: auto;
	padding-top: 7rem;
	padding-bottom: 7rem;
`;
