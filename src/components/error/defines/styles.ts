import { styled } from "@mui/material";

export const StyledErrorViewContainer = styled("div")({
	margin: "auto",
});

export const StyledErrorCode = styled("h2")({
	fontSize: 72,
	fontWeight: "bold",
	textAlign: "center",
});

export const StyledErrorTitle = styled("h3")({
	fontSize: 32,
	fontWeight: "semi-bold",
	textAlign: "center",
	marginTop: 24,
});

export const StyledErrorDescription = styled("p")({
	fontSize: 16,
	textAlign: "center",
	lineHeight: 1.5,
	marginTop: 16,
});
