import { StyledCentralBox } from "@/components/common/CentralBox/defines/styles";
import { styled } from "@mui/material";

export const StyledBoxInFindPasswordComplete = styled(StyledCentralBox)`
	display: flex;
	flex-direction: column;
	gap: 2rem;
`;
