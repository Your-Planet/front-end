import { InputAdornment, styled, TextField } from "@mui/material";

export const StyledPointerBox = styled(TextField)`
	cursor: pointer;

	& * {
		cursor: pointer;
	}
`;

export const StyledPointerAdornment = styled(InputAdornment)`
	cursor: pointer;

	& * {
		cursor: pointer;
	}
`;
